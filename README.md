# Semantic-Search
This project builds an AI-based semantic search system where documents are converted into embeddings and stored in MongoDB. The system performs hybrid search to return more relevant results through a Node.js backend and React frontend.
